# Replace and Remove pp74
# Time Complexity O(n), Space Complexity O(n)
from typing import List

def replace_and_remove(size:int, s: List[str]) -> int:
    print("hey")

my_list = ['a', 'a', 'b', 'c']
replace_and_remove(1, my_list)